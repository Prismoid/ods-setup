services:
  method-test-server:
    image: python:3.12-alpine
    container_name: method-test-server
    working_dir: /app
    command: ["python", "server.py"]
    volumes:
      - ./server.py:/app/server.py:ro
    ports:
      - "127.0.0.1:5050:5050"
    restart: unless-stopped